# Cursor Notes

This project can be installed into a Cursor-compatible local rules area with:

```powershell
python scripts\install_cursor.py --dry-run --json
python scripts\install_cursor.py --force
```

Use the public-safe Skill docs to guide learner work. Keep generated advice tied to the learner profile, ability profile, practice ledger, and error summary. Do not copy private prompt bodies into Cursor rules, chat output intended for sharing, or repository files.
