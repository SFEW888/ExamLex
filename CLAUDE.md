# Claude Code Notes

Use `skills/examlex/SKILL.md` as the entry point after installing with:

```powershell
python scripts\install_claude.py --dry-run --json
python scripts\install_claude.py --force
```

When operating the tutor, validate the learner profile, generate a daily plan, record practice, summarize errors, update ability, and feed the evidence into the next plan. Keep public-safe prompt placeholders in shared output and never publish the private eight prompt bodies.
